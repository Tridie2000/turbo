{
  "name": "product",
  "version": "1.0.0",
  "private": true,
  "description": "Product api",
  "scripts": {
    "build": "tsc && esbuild src/index.ts --bundle --platform=node --target=node22 --outfile=dist/bundle.js --sourcemap --minify",
    "dev": "tsx watch --clear-screen=false src/index.ts"
  },
  "dependencies": {
    "express": "^5.0.0",
    "sqlite": "*"
  }
}
